import React from 'react';
import Menu from './Menu'

const About = () => {
    return(
        <div className="container">
            <Menu />
            <div className="text-center">This is a simple react-redux application with routing</div>
        </div>
        );
}

export default About;